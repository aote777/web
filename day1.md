# day1

css的使用方法 
    内联样式、外联样式、行间样式
选择器
    通配符 *
    标签选择器 p
    class .p1
    id #d1
    直接选择器 >
    通用选择器 ~
    群组选择器 ,
    子选择器 空格
    伪类选择器 

​       .wrap .list-1 li:first-child{

       } 
       .wrap .list-1 li:nth-child(13){
    
       }
        .wrap .list-1 li:last-child{
    
       } 
文本样式
    颜色 对齐（text-align:center;） 字间距 首行缩进 大小写转换 文本阴影 处理空白符
字体
    字体系列（引用网上字体） 大小 字重 小型大写字体
背景
    qq空间 渐变色
盒模型（制作一个零件、零件内部距离、两个盒模型之间的距离 margin）
    制作零件  margin padding   小米盒模型练习
布局 position
    当你想让这个元素定位到页面的某一个位置
伪类、伪元素
hover 购物车（购物车的呼出部分要作为被hover元素的子元素）  iconfont
过渡 一种状态变化到另外一种状态的变化过程（执行一次） hover 执行 鼠标离开之后 会按照设计的变化方式再变回去
        一个属性的属性值变化到另外一个属性值的时候
转换 
    2d 旋转点 2维空间是以点为中心进行旋转  scale 视觉上的变化  不会挤开其他元素
    3d 3维空间的旋转的本质是轴旋转 当元素绕着某一个轴旋转的时候 他的轴的指向也发生了变化
         html部分构建三维空间 div三层嵌套
         视距视点   
         保留子元素的3d效果  preserve-3d    
         转换元素  旋转 平移
动画：可以多次执行的变化效果 保留动画最初的那一帧以及动画执行完毕的最后一帧 下一周期如何播放  alternative 正向执行完毕之后 逆向执行回来 如此反复
媒体查询
栅格系统
响应式布局
less